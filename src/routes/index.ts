import type { ROUTE } from '@/types';

const routes: ROUTE[] = [
  {
    name: 'About',
    path: '/about',
  },
  {
    name: 'Topics',
    path: '/topics',
  },
  {
    name: 'Utils',
    path: '/util-funcs',
  },
];

export default routes;
